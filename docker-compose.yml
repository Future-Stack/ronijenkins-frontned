services:
  sullerk-client:
    image: ${DOCKER_USERNAME:-local}/navelle-frontend:latest
    container_name: navelle-frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    restart: unless-stopped